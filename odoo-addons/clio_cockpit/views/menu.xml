<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- Toppnivå — ingen action, bara behållare -->
  <menuitem id="menu_clio_cockpit_root"
            name="Clio Cockpit"
            sequence="85"/>

  <!-- Undermenyer — synliga för alla inloggade -->
  <menuitem id="menu_cockpit_rag"
            name="🔍 RAG"
            parent="menu_clio_cockpit_root"
            action="action_cockpit_rag"
            sequence="10"/>

  <menuitem id="menu_cockpit_bibliotek"
            name="📚 Bibliotek"
            parent="menu_clio_cockpit_root"
            action="action_cockpit_bibliotek"
            sequence="20"/>

  <!-- Synliga för Clio User -->
  <menuitem id="menu_cockpit_heartbeat"
            name="💓 Agenthälsa"
            parent="menu_clio_cockpit_root"
            action="clio_cockpit.action_clio_tool_heartbeat"
            groups="clio_cockpit.group_clio_user"
            sequence="35"/>

  <!-- Synliga för Clio Admin -->
  <menuitem id="menu_cockpit_server"
            name="🖥️ Server"
            parent="menu_clio_cockpit_root"
            action="action_cockpit_server"
            groups="clio_cockpit.group_clio_admin"
            sequence="50"/>

</odoo>
